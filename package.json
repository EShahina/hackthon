{
  "name": "reelsense",
  "version": "1.0.0",
  "description": "ReelSense — AI Semantic Video Recommendation Agent",
  "type": "module",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "start": "npx serve . -l 3000"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}